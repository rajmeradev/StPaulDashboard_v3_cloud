[build]
builder = "nixpacks"
buildCommand = "cd frontend && npm install && npm run build && cd .."

[deploy]
startCommand = "uvicorn tools.api_bridge:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/"
restartPolicyType = "on-failure"
