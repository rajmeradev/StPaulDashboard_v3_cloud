services:
  - type: web
    name: st-paul-dashboard
    runtime: python
    buildCommand: ./build.sh
    startCommand: uvicorn tools.api_bridge:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9
      - key: NODE_VERSION
        value: 20
